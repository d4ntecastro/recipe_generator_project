# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python/
env/
venv/
*.env
.idea/ # For PyCharm/IntelliJ users
.vscode/ # For VS Code users
db.sqlite3 # Your local Django database file
/media/ # User-uploaded media files (should be handled by cloud storage in production)
*.log
*.sqlite3
.DS_Store # macOS specific

# Pipenv
Pipfile.lock # This is debatable, but often kept out to let CI generate it. For simplicity, we'll keep it out for now.
.venv/ # Pipenv's default virtual environment directory

# Node.js / React
/frontend/node_modules/
/frontend/dist/ # Vite's build output folder
.env.local # Local environment variables for React
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm/
.yarn/

# Gemini API Key (VERY IMPORTANT - DO NOT COMMIT YOUR API KEY)
# If you put your API key directly in a file (which is not recommended),
# make sure that file is listed here.
# For example, if you had a file named 'api_keys.py' with your key:
# api_keys.py