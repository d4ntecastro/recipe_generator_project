 # Python specific ignores
    __pycache__/
    *.pyc
    *.pyo
    *.pyd
    env/
    venv/
    .venv/          # Pipenv's default virtual environment directory
    *.log
    *.sqlite3       # Your local Django database file
    /media/         # User-uploaded media files (should be handled by cloud storage in production)

    # Django specific ignores
    local_settings.py # If you use local settings that override production ones

    # Pipenv specific ignores
    Pipfile.lock    # <--- ADD THIS LINE TO IGNORE Pipfile.lock

    # Node.js / Frontend specific ignores
    /frontend/node_modules/
    /frontend/dist/ # Vite's build output folder
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*
    .pnpm-debug.log*
    .npm/
    .yarn/

    # Environment variables
    .env
    .env.local
    .env.*.local
    # If you put your Gemini API key in a file named 'gemini_api_key.txt' for example:
    # gemini_api_key.txt

    # Operating System specific ignores
    .DS_Store       # macOS specific
    Thumbs.db       # Windows specific
    ehthumbs.db     # Windows specific
    Desktop.ini     # Windows specific

    # IDE specific ignores
    .idea/          # JetBrains IDEs (PyCharm, IntelliJ)
    .vscode/        # VS Code
    *.sublime-project
    *.sublime-workspace